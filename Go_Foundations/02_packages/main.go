package main

import (
	"fmt"
	"math"
)

func add(x, y int) int {
	return x + y
}

func main() {
	fmt.Println("Hello, just getting started with math libary!")
	fmt.Println(math.Sqrt(36))
	fmt.Println(add(12, 13))
}
